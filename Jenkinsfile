pipeline{
    agent any
    stages {
        stage('Build') {
            steps {
                // Run the npm build
                sh'node --version'
                sh'npm --version'
                sh 'npm install'
            }
        }
    }
}    
